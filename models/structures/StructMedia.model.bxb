structure (StructMedia) {
  description (structure of 'mediatype' table in db)
  property (mediaID) {
    // id
    type (TypeMediaID)
    min (Required) max (One)
  }
  property (mediaName) {
    // Name
    type (TypeMediaName)
    min (Required) max (One)
  }
}

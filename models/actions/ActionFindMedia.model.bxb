action (ActionFindMedia) {
  description (find media according to id, if no id, list all entry)
  type (Search)
  collect {
    input (media_id) {
      type (TypeMediaID)
      min (Optional) max (One)
    }
  }
  output (StructMedia)
}
